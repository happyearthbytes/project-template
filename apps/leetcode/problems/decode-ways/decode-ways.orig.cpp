// A message containing letters from <code>A-Z</code> can be <strong>encoded</strong> into numbers using the following mapping:</p>
class Solution {
public:
    int numDecodings(string s) {
        
    }
};

